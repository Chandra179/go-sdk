# =============================================================================
# Vault Connection Configuration (Local Development)
# =============================================================================
# This file contains Vault connection settings for local development.
# In production, these should be configured via your orchestration platform.
# 
# Copy this file to .env.vault and update values as needed.
# =============================================================================

# Vault Server Address
# For Docker Compose: use the service name
# For local Vault dev mode: http://localhost:8200
VAULT_ADDR=http://localhost:8200

# Vault Authentication Token
# In dev mode: use "root"
# In production: use AppRole, Kubernetes auth, or other method
VAULT_TOKEN=root

# Vault Token File Path (for Vault Agent)
VAULT_TOKEN_PATH=./vault/token/root-token

# =============================================================================
# Optional: Direct Secret Values (for testing without Vault)
# =============================================================================
# Uncomment and set these ONLY for local testing without Vault.
# These values will be loaded directly when Vault is not available.

# POSTGRES_PASSWORD=devpassword
# REDIS_PASSWORD=redispass
# JWT_SECRET=your-jwt-secret-must-be-at-least-32-characters-long
# GOOGLE_CLIENT_ID=your_google_client_id_here
# GOOGLE_CLIENT_SECRET=your_google_client_secret_here
# RABBITMQ_URL=amqp://guest:guest@localhost:5672/
