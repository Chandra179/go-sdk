apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-datasources
  namespace: observability
data:
  datasources.yml: |
    apiVersion: 1

    datasources:
      # Loki for logs
      - name: Loki
        type: loki
        access: proxy
        uid: loki  # Add explicit UID
        url: http://loki.observability.svc.cluster.local:3100
        isDefault: false
        editable: true
        jsonData:
          derivedFields:
            - datasourceUid: jaeger  # This now matches the Jaeger UID below
              matcherRegex: "trace_id=(\\w+)"
              name: TraceID
              url: "$${__value.raw}"

      # Prometheus for metrics
      - name: Prometheus
        type: prometheus
        access: proxy
        uid: prometheus  # Add explicit UID
        url: http://prometheus.observability.svc.cluster.local:9090
        isDefault: true
        editable: true

      # Jaeger for traces
      - name: Jaeger
        type: jaeger
        access: proxy
        uid: jaeger  # Add explicit UID to match the reference above
        url: http://jaeger.observability.svc.cluster.local:16685
        isDefault: false
        editable: true
        jsonData:
          tracesToLogsV2:
            datasourceUid: loki  # This now matches the Loki UID above
            spanStartTimeShift: "-1h"
            spanEndTimeShift: "1h"
            filterByTraceID: true
            filterBySpanID: false
            tags:
              - key: "trace_id"
                value: "trace_id"